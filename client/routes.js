/*Router.configure({
  // the default layout
  layoutTemplate: 'root'
});

Router.route('/', function () {
  this.render('home');
});

Router.route('/profile', function () {
  this.render('profile');
});*/
